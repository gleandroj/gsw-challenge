{
  "name": "App",
  "engines": {
    "node": "8.1.x"
  },
  "scripts": {
    "postinstall": "npm install --prefix api && npm run prod --prefix api && npm install --prefix front && npm run build --prefix front -- --prod && mv front/build api/dist/public"
  }
}